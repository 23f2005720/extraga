name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Run a simple command
      run: echo "Hello, World!"
    
    - name: Run a multi-line script
      run: |
        echo "This workflow is running successfully"
        echo "Current date: $(date)"
